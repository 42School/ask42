## Ask42


#### Setup

To git clone and launch server:

`git clone https://github.com/lguenier/ask42.git`

Now, configure your database settings in `config/database.yml` file, and then

`cd ask42 && bundle && rails s`

#### About

@TODO @lguenier 
